import apiClient from './axios';

// TODO: implementar servicios para catálogo de coreografías y videos

export const ChoreographyService = {
  // list: (params) => apiClient.get('/choreographies/', { params }),
};

