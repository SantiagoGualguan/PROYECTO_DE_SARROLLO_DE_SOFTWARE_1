# ── Base de datos local (Docker) ───────────────────────
POSTGRES_DB=dancelearn_db
POSTGRES_USER=dancelearn_user
POSTGRES_PASSWORD=cambia_esto_por_password_seguro

# ── Django ─────────────────────────────────────────────
SECRET_KEY=cambia_esto_por_clave_secreta_larga
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_SETTINGS_MODULE=config.settings.development

# ── Base de datos producción (Supabase) ─────────────────
# DATABASE_URL=postgresql://user:pass@db.ref.supabase.co:5432/postgres

# ── Frontend ────────────────────────────────────────────
VITE_API_URL=http://localhost:8000/api
# En producción (Vercel): VITE_API_URL=https://tu-backend.onrender.com/api

